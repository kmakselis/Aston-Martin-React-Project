import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff',
    },
  },
  shape: {
    borderRadius: 4,
  },
  // typography: {
  //   fontFamily: ubuntu,
  // },
});

export default theme;
